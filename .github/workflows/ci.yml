name: CI

on: push

jobs:
  e2e-test:
    strategy:
      matrix:
        provider: [vercel, netlify, cloudflare, 'local']
    uses: jill64/playwright-config/.github/workflows/run-playwright.yml@main
    with:
      test-command: npx playwright test
      hosting-provider: ${{ matrix.provider }}
