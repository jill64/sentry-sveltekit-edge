name: CI

on: push

jobs:
  e2e-test:
    strategy:
      matrix:
        provider: [vercel, cloudflare, local]
    uses: jill64/playwright-config/.github/workflows/run-playwright.yml@79be820c194e134cd595ded9bd0258f2297175f3 # v2.4.0
    with:
      test-command: npx playwright test
      hosting-provider: ${{ matrix.provider }}
