name: CI

on: push

jobs:
  e2e-test:
    strategy:
      matrix:
        provider: [vercel, cloudflare, local]
    uses: jill64/playwright-config/.github/workflows/run-playwright.yml@374b1a370d028014cc9f0a1209921007ceb69018 # v2.2.2
    with:
      test-command: npx playwright test
      hosting-provider: ${{ matrix.provider }}
