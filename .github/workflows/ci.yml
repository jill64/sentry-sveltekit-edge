name: CI

on: push

jobs:
  e2e-test:
    strategy:
      matrix:
        provider: [vercel, cloudflare, local]
    uses: jill64/playwright-config/.github/workflows/run-playwright.yml@0da2bf603718b869daea0654fad791e43c6ea777 # v2.2.3
    with:
      test-command: npx playwright test
      hosting-provider: ${{ matrix.provider }}
